const longestIncreasingSubsequence = inputArray => {
  if (!Array.isArray(inputArray)) {
    throw TypeError('Expected array as an argument.');
  }

  return [];
};

module.exports = longestIncreasingSubsequence;
